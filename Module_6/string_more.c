#include <stdio.h>
#include <string.h>
#include <cs50.h>
#include <ctype.h>

//function prototypes
bool is_palindrome(string word);

// main script
int main(void)
{
    is_palindrome("Blaalb")
}

//functions
bool is_palindrome(string input)
{
    
    // calc string length
    length = strlen(input);
 
    // initial variables
    backwards_counter = length;
    
    for(int character = 0; character < length; character++)
    {
        
    }
    
}
